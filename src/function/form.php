<?php
require_once "../class/newTable.php";
require_once "../class/createTable.php";
/*if ($_SERVER["REQUEST_METHOD"] == "POST") {
  if (!empty($_POST["title"])) {
    $title = $_POST["title"];

  }
  else {
    $er_title = "Title is not empty";
  }

  if (!empty($_POST["message"])) {
    $message = $_POST["message"];
  }
  else {
    $er_message = "Message is not empty";
  }

  $query_con = $db->addArticle($title, $message);
  header("Location: /");
}*/

//header("Location: /");
